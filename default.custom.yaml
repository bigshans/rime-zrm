# default.custom.yaml
# save it to: 
#   ~/.config/ibus/rime  (linux)
#   ~/Library/Rime       (macos)
#   %APPDATA%\Rime       (windows)

patch:
  schema_list:
    - schema: zrm_pinyin        # 自然码+辅助码
  menu/page_size: 9
  custom_phrase/enable_completion: true
  # key_binder/bindings:
    # - {accept: Escape, toggle: ascii_mode, when: always}
